import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#6C3ED4',
    },
    secondary: {
      main: '#EBC20C',
    },
    background: {
      default: '#F1E6EA',
    },
  },
  typography: {
    fontFamily: 'Lato, sans-serif',
  },
});
